version: "3.8"

services:
  visualizador:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: visualizador-sistemas
    environment:
      PORT: 3000
      SESSION_SECRET: troque-essa-chave-no-portainer
    ports:
      - "38520:3000"
    volumes:
      - painel_data:/app/data
    restart: unless-stopped

volumes:
  painel_data:
